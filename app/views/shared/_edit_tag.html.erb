<div class='editTag' draggable='true'>

<% @tag = @media_object.tag %>

<%if @tag%>

<%= form_for(@tag, remote: true) do |f| %>
  <% if @tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tag.errors.count, "error") %> prohibited this tag from being saved:</h2>

      <ul>
      <% @tag.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<%=link_to "Delete Notice", delete_tag_media_object_path(@media_object), remote: true %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description, rows: 10 %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%else%>
<%=link_to "New Notice", new_tag_media_object_path(@media_object), remote: true %>
<% end %>

</div>
